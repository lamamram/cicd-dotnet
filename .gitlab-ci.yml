image: "$CI_REGISTRY_IMAGE/dotnet-sdk:6.0"

stages:
  - triggers

trigger1:
  stage: triggers
  trigger:
    include: projet1/cicd.yml

trigger2:
  stage: triggers
  trigger:
    include: projet2/cicd.yml