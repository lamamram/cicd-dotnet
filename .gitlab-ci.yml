image: "$CI_REGISTRY_IMAGE/dotnet-sdk:6.0"

stages:
  - builds
  - tests

build:
  stage: builds
  before_script:
    - dotnet restore
  script:
    - dotnet build --no-restore

build2:
  stage: builds
  script:
    - echo "Build2"

test:
  stage: tests
  script:
    - echo "Test"

test2:
  stage: tests
  script:
    - echo "Test"

