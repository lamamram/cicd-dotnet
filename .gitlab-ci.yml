image: "$CI_REGISTRY_IMAGE/dotnet-sdk:6.0"

stages:
  - builds
  - tests
#  - deploys


.build:
  stage: builds
  script:
    - dotnet publish -c Release -o publish MyProject/MyProject.csproj
    - ls -al publish


test:
  stage: tests
  script:
    - dotnet test
