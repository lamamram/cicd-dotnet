image: "$CI_REGISTRY_IMAGE/dotnet-sdk:6.0"

# ordre d'exécution des stages
stages:
  - builds
  - tests

build:
  # attachement d'un job à un stage
  stage: builds
  before_script:
    - dotnet restore
  script:
    - dotnet build --no-restore


test:
  stage: tests
  script:
    - echo "Test"

