build:
  script:
    # exécuter 3 commandes avec une ligne de script
    - |
      echo "Build"
      pwd
      uname -a

test:
  script:
    # exécuter 1 commande répartie sur plusieurs lignes
    - >
      echo "Commande arbitrairement longue" &&
      echo " qui risque de déborder en largeur"